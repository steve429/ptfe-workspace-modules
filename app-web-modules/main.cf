provider "aws" {
}

module "server" {
  source  = "app.terraform.io/frb-sb/server/aws"
  version = "0.1.0"

  ami                    = var.ami
  subnet_id              = var.subnet_id
  vpc_security_group_ids = var.vpc_security_group_ids
  identity               = var.identity
  web_count              = var.web_count
}

